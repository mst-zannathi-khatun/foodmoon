import 'package:flutter/material.dart';

/// Style for the primary location text in the app bar.
TextStyle appBarPrimaryLocationStyle = const TextStyle(
  color: Colors.white,
  fontSize: 16,
  fontWeight: FontWeight.bold,
  letterSpacing: 1.50,
);

/// Style for the secondary location text in the app bar.
TextStyle appBarSecondaryLocationStyle = const TextStyle(
  color: Colors.white,
  fontSize: 12,
);

/// Style for the searchHitText in the home page search bar.
TextStyle homePageSearchBarHintText = TextStyle(
  letterSpacing: 1.25,
  color: Colors.grey.withOpacity(0.9),
);

TextStyle homeScreensRecommendedOrder = const TextStyle(
    fontWeight: FontWeight.bold, fontSize: 16,);
