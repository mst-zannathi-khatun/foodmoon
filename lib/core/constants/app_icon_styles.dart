import 'package:flutter/material.dart';

class AppIconStyle {
  /// Style for the favoriteIcon in the app bar.
  static Icon favoriteIcon = const Icon(Icons.favorite_border, color: Colors.white, size: 22);
  /// Style for the addToCard in the app bar.
  static Icon addToCard = const Icon(Icons.add_card_rounded, color: Colors.white, size: 22);
  /// Style for the menuIcon in the app bar.
  static Icon menuIcon = const Icon(Icons.menu, color: Colors.white, size: 27);
  /// Style for the search in the home page search bar.
  static Icon search = const Icon (Icons.search, color: Colors.grey, size: 33,);

}
